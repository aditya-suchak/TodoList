<!-- <div class="block mb-2 text-white">
  <%= task.description %>
</div> mr-2 align-middle bg-gray-50 border-gray-300 rounded checked:bg-gray-400 enabled:gray-400 hover:bg-gray-600 active:bg-gray-600 enabled:bg-gray-600 -->


<div class="block">
  <%= form_with(model: task, class:"text-lg inline-block my-3 w-72 text-white") do |form| %>
    <%= form.check_box :completed,
                       data: {
                         id: task.id,
                         action: "tasks#toggle"
                       },
                       class: "rounded bg-gray-600 mr-2 align-middle checked:bg-gray-400" %>
    <%= task.description %>
  <% end %>

  <%= link_to edit_task_path(task), class: "btn bg-gray-100" do %>
    Edit
  <% end %>


  <div class="inline-block ml-2">
    <%= button_to "Delete", task_path(task),
                  method: :delete,
                  class: "btn bg-red-100" %>
  </div>
</div>
